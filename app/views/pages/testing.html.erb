<head>
  <style>
  .carousel-item {
    min-height: 100vh;
    min-width: 100vw;
    transition: transform 0.9s ease-in-out;

  }

  .carousel-item.active {
    /* Add styles for the active item */
    transform: translateX(-100vw);
}

  .carousel-item:not(.active) {
      /* Add styles for the non-active items */
      transform: translateX(0vw);
  }



  </style>
</head>

<div id="layout" style="width: 100vw; height: 100vh; position: relative; display: flex; justify-content: center">
  <div id="logo" style="background-color: black; color: white; border-radius: 50px; position: absolute; bottom: 10vh; z-index: 2">
  cute logo
  </div>

  <button id="next-button" style="position: absolute; right: 10vw; top: 49vh; z-index: 2">></button>
  <button id="prev-button" style="position: absolute; left: 10vw; top: 49vh; z-index: 2"><</button>

  <div  style = " width: 100vw; overflow: scroll; ">
    <div id="carousel-container" style="position: relative; right: 0; display: flex;">
    <div id="carousel-1" class = "carousel-item" style="background-color: green">.</div>
    <div id="carousel-2" class = "carousel-item" style="background-color: blue">.</div>
    <div id="carousel-3" class = "carousel-item" style="background-color: red">.</div>
    </div>
  </div>

</div>


<script>
    // Get the button
    var nextButton = document.querySelector("#next-button");
    // Add click event listener to the button
    nextButton.addEventListener("click", function(){
        // Code to navigate to next item in carousel goes here
        carouselContainer = document.querySelector("#carousel-container")
        console.log(carouselContainer.style.right)
        carouselContainer.classList.add("active")
        carouselContainer.style.right = "100vw"
    // Calculate the new position


    });





//     var element = document.getElementById("logo");
// var x = 0;
// var y = 0;
// var speed = 5;

// function animate() {

//   x += speed;
//   y += speed;
//   element.style.left = x + "px";
//   element.style.top = y + "px";

//   //requestAnimationFrame(animate);
//   setTimeout(function() {console.log("1 second has passed");}, 1000);

//   }

// while (element.style.left != "50px") {requestAnimationFrame(animate);
// }

</script>
